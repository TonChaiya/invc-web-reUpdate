@model string
@* Localized per-section failure (DashboardSection<T>.IsAvailable == false): small warning, never a full-page alert. *@
<div class="dashboard-unavailable" role="status">
    <strong>@Model</strong> — ส่วนนี้อ่านข้อมูลไม่สำเร็จ · ดู <a asp-page="/Health">สถานะระบบ</a>
</div>
