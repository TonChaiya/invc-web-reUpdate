@model string
<div class="metric-card metric-unavailable" role="status">
    <span class="metric-label">@Model</span>
    <span class="metric-value metric-na">ไม่พร้อมใช้งาน</span>
    <span class="metric-foot">ส่วนนี้อ่านข้อมูลไม่สำเร็จ — ดู <a asp-page="/Health">สถานะระบบ</a></span>
</div>
