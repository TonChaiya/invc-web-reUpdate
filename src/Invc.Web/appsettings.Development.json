{
  "DetailedErrors": true,
  "InvDatabase": {
    "_comment": "Developer default: Windows authentication to the local default instance. Override with InvDatabase__ConnectionString if your dev box differs.",
    "ConnectionString": "Server=.;Database=INV;Integrated Security=true;Encrypt=false;TrustServerCertificate=true"
  },
  "AppDatabase": {
    "_comment": "Application-owned MySQL (invc_web) for the Borrow module. Put the real connection string in the untracked appsettings.Development.local.json (git-ignored) or AppDatabase__ConnectionString. Never commit credentials.",
    "ConnectionString": ""
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  }
}
